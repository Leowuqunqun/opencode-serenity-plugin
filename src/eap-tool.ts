/**
 * eap-tool.ts — ACC native tool
 *
 * Progressive disclosure: tool description is minimal ("when to use"),
 * full content is returned on execution. This preserves skill-like
 * loading behavior within the ACC framework.
 *
 * ACC/CCC model: eap is an ACC primitive. Every CCC gets it automatically.
 */

import { tool, type ToolDefinition } from '@opencode-ai/plugin';
import pkg from '../package.json' with { type: 'json' };

const VERSION = pkg.version;

const CONTENT = `# EAP — Explicit Abstraction Principle

## 用途

EAP（Explicit Abstraction Principle，显式抽象原则）是一个关于"思维在 LLM 时代如何获得功能价值"的理论框架。本 skill 将 EAP 理论注入 Agent 行为模式，在感知到用户表达显式度不足时主动介入，引导用户完成隐式→显式的转化。

## 什么是 EAP

EAP（Explicit Abstraction Principle，显式抽象原则）是一个关于"思维在 LLM 时代如何获得功能价值"的理论框架。其核心命题是：

> **思维的功能价值，与其外部可重建性成正比。**

在 LLM 主导的认知环境中，思维如果停留在内部、不可外部化，就无法被 LLM 接收、处理和执行——它变得不可操作。因此，**构建显式抽象的能力**正在成为 LLM 时代最关键的元能力。

本 skill 将 EAP 理论注入 Agent 行为模式，使 Agent 在感知到用户表达显式度不足时，主动介入并引导用户完成隐式→显式的转化。

---

## 触发条件/何时加载

当以下信号出现时，你应当加载本 skill 并进入 EAP 模式：
- 用户表达中缺少关键变量定义（E↓）
- 用户表达中存在大量隐含假设（R↑）
- 同一段话可以有两种以上合理解释（S↓）
- 用户使用了高歧义词汇且上下文未锁定含义

### 显式度不足的信号

| 信号 | 用户输入示例 | EAP 判断 |
|------|------------|---------|
| 缺少关键变量 | "系统应该快" | 没有定义"快"的量化指标，E 极低 |
| 隐含假设 | "用常规方案就行" | "常规"是什么？不同人的理解不同，S 极低 |
| 依赖共享上下文 | "你懂的，就是那个" | LLM 没有你的共享上下文，无法重建 |
| 层级跳跃 | "给我做个登录" | 从最高抽象直接跳到实现，中间缺少多层约束 |
| 模糊关系 | "A 和 B 关联" | 关联类型/方向/基数/条件均未定义 |
| 压缩表达 | "加个字段" | 字段名/类型/默认值/含义均未指定 |
| 歧义词汇 | "处理一下这个问题" | "处理"是修 bug/加功能/改配置/删数据？"问题"是 bug/需求/疑问？ |
| 缺失边界 | "支持更多用户" | 多少算"更多"？从多少到多少？场景是什么？ |
| 缺乏验收标准 | "做好就行" | "好"的标准是什么？谁来判断？ |
| 过早细节 | 在高层抽象未对齐时讨论具体实现 | 控制论陷阱：过早进入"如何做"而跳过"是什么/为什么" |

---

## EAP 核心框架

### 三个核心变量

在评估任何认知表达时，使用这三个维度：

| 变量 | 含义 | 低值表现 | 高值表现 |
|------|------|---------|---------|
| **E** (显式度) | 有多少结构被外部编码 | 依赖隐含上下文，变量未定义 | 变量明确，关系编码，步骤可恢复 |
| **R** (重建成本) | 外部系统恢复意义所需的代价 | 需要大量猜测/补全/追问 | 可被直接解析和执行 |
| **S** (稳定性) | 不同系统对同一表达的重建一致程度 | 十个人有十种理解 | 所有接收者得到相同的结构 |

**EAP 原则**：功能价值随 E↑、R↓、S↑ 而最大化。

---

## EAP v2 认知质量管道

EAP v2 引入完整的认知质量管道，从输入到输出经过多层检查：

\`\`\`
输入文本
  │
  ├── DIG（检查触发层）
  │    └─ 5 项量化指标判断是否应进入 EAP 模式
  │
  ├── 感知层（问题分析）
  │    └─ 识别歧义词汇、缺失变量、隐含假设
  │
  ├── 评估层（质量评分）
  │    └─ 16 项显式/隐式抽象检查清单
  │
  ├── 修正层（改进建议）
  │    └─ E/R/S 三变量启发式诊断 + 具体改进方案
  │
  ├── 方案评估层（置信度评估）
  │    └─ 评估修正方案的置信度
  │
  └── 信度链（总置信度 = 各层置信度的乘积）
       └─ 任一环节置信度低于阈值（0.5）则阻断
\`\`\`

### 置信度乘积模型

总置信度 = C₁ × C₂ × ... × Cₙ（各层置信度的乘积）

- 任一环节置信度 < 0.5 → 自动阻断，返回上一层级继续修正
- Agent 可以根据置信度判断是否推进到下一阶段

---

### 显式抽象 vs 隐式抽象 Checklist

#### 显式抽象 Checklist（满足条件越多，越接近显式抽象）

- [ ] 所有变量/实体都有**明确定义**（名称、类型、取值范围）
- [ ] 所有关系都有**结构性编码**（类型、方向、基数、条件）
- [ ] 推理步骤可被外部系统**逐步恢复**
- [ ] 表达**不依赖**任何特定接收者的内部上下文
- [ ] 边界和范围有**显式说明**（什么是、什么不是）
- [ ] 验收标准有**可验证的描述**（可测试、可量化）
- [ ] 假设和约束有**明确列举**
- [ ] 不存在**歧义词汇**（如"优化"、"处理"、"问题"——除非已在上下文中锁定含义）

#### 隐式抽象 Checklist（满足条件越多，越接近隐式抽象）

- [ ] 变量/实体依赖**共享上下文**才能理解
- [ ] 关系被**压缩或省略**（如"关联"而不说1:1/1:N/多对多）
- [ ] 推理步骤需要接收者**内部补全**
- [ ] 表达**强依赖**发送者意图，接收者无法独立重建
- [ ] 边界和范围**不明确**（如"更多"、"更好"、"常规方案"）
- [ ] 验收标准**主观或不可验证**
- [ ] 假设和约束**未显式列举**
- [ ] 使用大量**歧义词汇**且上下文未锁定

---

## EAP 实践指南

### 第一步：诊断当前表达的 E/R/S 状态

快速评估用户当前表达的三个变量水平：

\`\`\`
E: __ (低/中/高)
R: __ (高/中/低)   ← 注意 R 与 E 反向
S: __ (低/中/高)
\`\`\`

### 第二步：识别缺失的结构要素

**通用要素**：
- [ ] 变量/实体是否明确定义
- [ ] 关系/依赖是否指明方向、基数、条件
- [ ] 边界/范围是否划定（什么是，什么不是）
- [ ] 验收标准/成功条件是否明确
- [ ] 约束条件是否列出

**技术任务要素**：
- [ ] 接口签名（入参/出参/错误码）
- [ ] 数据类型和格式
- [ ] 默认值和边界情况
- [ ] 性能指标（量化）
- [ ] 兼容性要求

**设计任务要素**：
- [ ] 目标和范围（含非目标）
- [ ] 关键场景/用户故事
- [ ] 决策记录和取舍原因
- [ ] 开放问题和已关闭决策

### 第三步：用结构性质问引导显式化

| 质问模式 | 触发条件 | 质问示例 |
|---------|---------|---------|
| **变量质问** | 用户提到某事物但未定义其名称/类型/取值 | "你说的'字段'具体是指什么名称？什么类型？" |
| **关系质问** | 用户提到两个以上实体但未定义关系 | "A 和 B 之间的关联是 1:1 还是 1:N？方向是 A→B 还是双向？" |
| **边界质问** | 用户使用模糊量词（更多/更好/常规） | "你说的'支持更多用户'，当前是多少？目标是多少？什么时间范围内？" |
| **量化质问** | 用户使用模糊形容词（快/慢/大/小） | "'快'的具体指标是什么？P95 < 200ms 还是 P99 < 1s？" |
| **歧义质问** | 用户使用高歧义词汇（处理/优化/问题） | "这里的'处理'是指修复 bug、添加功能、还是修改配置？" |
| **层级质问** | 用户从高层抽象直接跳到低层实现 | "你说的'做个登录功能'，我们先对齐一下：使用什么认证方式（手机号/邮箱/SSO）？" |
| **条件质问** | 用户提到某动作但未定义触发条件 | "如果出错就重试"——当什么情况算"出错"？HTTP 5xx？4xx 也算？" |
| **范围质问** | 用户使用开放式范围（最近/经常/部分） | "你说'部分情况'，具体是哪部分？占比多少？哪些场景？" |

### 第四步：提供"英式中文"改写建议

| 问题 | 用户原文 | 英式中文改写 |
|------|---------|-------------|
| 变量未定义 | "加个字段" | "增加一个名为 preferences 的字段，类型为 JSON Object，默认值为 {}" |
| 时间未指定 | "定期清理数据" | "每 24 小时执行一次数据清理，保留最近 90 天的数据" |
| 数量未量化 | "很多用户反馈" | "过去 7 天内有 23 条用户反馈记录，涉及约 15 个独立用户" |
| 条件未明确 | "如果出错就重试" | "当 HTTP 状态码为 5xx 时，最多重试 3 次，间隔为 1s/4s/9s（指数退避），4xx 不重试" |
| 对象未指定 | "优化一下性能" | "针对首页弹窗 API，将 P95 响应时间从当前 800ms 降低到 200ms 以下" |

### 第五步：确认层级完整性

\`\`\`
抽象链（从高到低）：
  需求/目标层     → "为什么要做？" 
  范围/边界层     → "做什么，不做什么？"
  方案/设计层     → "怎么做？有哪些选择？"
  接口/契约层     → "模块之间的交互是什么？"
  实现/代码层     → "具体怎么写？"
\`\`\`

---

## EAP 对话示例

### 示例 1：从模糊需求到结构化表达

**用户**：做一个公告已读统计功能。

**EAP 响应**：

> 我注意到你的需求中缺少几个关键变量，我们先逐步对齐：
>
> **问题 1**：公告出现在哪些位置？
> - [ ] 首页弹窗
> - [ ] 消息中心
> - [ ] 工作台卡片
> - [ ] 移动端
>
> **问题 2**："已读"的判断标准是什么？
> - [ ] 点击公告标题即算已读
> - [ ] 关闭弹窗即算已读
> - [ ] 在公告详情页停留超过 X 秒
>
> **问题 3**：需要哪些统计维度？
> - [ ] 单条公告的总已读数
> - [ ] 每个用户的已读/未读状态
> - [ ] 按时间范围的阅读趋势
>
> 每明确一个问题，我们的实现就会减少一次返工风险。

### 示例 2：从歧义词汇到精确术语

**用户**：把接口优化一下。

**EAP 响应**：

> "优化"可以有多种含义。我帮你拆分成几个具体选项，你确认方向后我们再深入：
>
> 1. **性能优化**：降低响应时间 / 减少数据库查询次数
> 2. **接口重构**：合并重复接口 / 统一返回格式 / 拆分过大的接口
> 3. **代码清理**：删除死代码 / 提升可读性 / 增加注释
> 4. **安全加固**：增加参数校验 / 添加鉴权 / 防止 SQL 注入
>
> 你的"优化"是其中哪一项？还是组合？不同的方向需要不同的方案。

### 示例 3：从跳级讨论到层级对齐

**用户**：在用户表加个 phone 字段，类型 varchar(20)。

**EAP 响应**：

> 我注意到你直接给出了实现细节（表 + 字段 + 类型），但在开始修改数据库之前，有几个更高层级的决策需要先对齐，以确保实现不会反复改动：
>
> **需求层**：为什么需要 phone 字段？
> - [ ] 手机号登录
> - [ ] 短信通知
> - [ ] 用户资料展示
> - [ ] 账号找回
>
> **方案层**：
> - 数据来源：用户自行填写？从已有系统同步？手机号唯一吗？
> - 格式要求：E.164 国际格式？支持国家码？
> - 验证方式：需要短信验证吗？验证码有效时长？
>
> 这些决策在需求层和方案层对齐后，实现层（数据库字段）的改动会非常稳定，不需要反复修改。

---

## EAP 与 Neat 的关系

- **Neat** 是 EAP 理论的**实践方法论**——它定义了一套可操作的协作流程（小步对齐、显式决策、文档驱动），适用于需要与用户共同构建设计文档的场景。
- **EAP** 是 Neat 的**理论基础**——它解释了为什么 Neat 有效（因为 Neat 的结构性质问提升了表达的 E 值、降低了 R 值、提高了 S 值）。

**选择指南**：
- 如果你发现用户表达的**显式度不足、隐式假设过多、难以重建**，使用 EAP 进行诊断和引导
- 如果你需要和用户**共同构建一份设计文档/计划/需求文档**，使用 Neat 进行协作
- 当两者都适用时，EAP 应先于 Neat 使用——先用 EAP 评估信息质量，再用 Neat 的流程推进文档化
`;

export const eapTool: ToolDefinition = tool({
  description:
    `EAP — Explicit Abstraction Principle theory, framework, practice guide, and examples. ` +
    `Progressive disclosure — ` +
    `call without arguments to receive the full SKILL.md content. ` +
    `(serenity-plugin v${VERSION})`,
  args: {},
  execute: async () => {
    return CONTENT;
  },
});
